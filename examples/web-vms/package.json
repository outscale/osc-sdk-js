{
  "name": "web-vms",
  "version": "1.0.0",
  "description": "Example of using BSU volumes in Outscale cloud",
  "main": "src/index.js",
  "scripts": {
    "build": "tsc && webpack",
    "prepare": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^3.0"
  },
  "dependencies": {
    "outscale-api": "file:../..",
    "webpack-cli": "^4.9.2"
  }
}
